# Captcha Provider Configuration

Store captcha provider notes and API keys in `sms.md`.

Example format for 2captcha:

```text
2captcha.com
API Key: YOUR_2CAPTCHA_API_KEY
```

Do not commit the real `sms.md` file.
